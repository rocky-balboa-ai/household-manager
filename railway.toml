# Monorepo config - services should set Root Directory to / and use these commands:
#
# WEB SERVICE:
#   Build: pnpm install --frozen-lockfile && pnpm --filter web build
#   Start: pnpm --filter web start
#
# API SERVICE:
#   Build: pnpm install --frozen-lockfile && pnpm --filter api build
#   Start: pnpm --filter api start:prod
