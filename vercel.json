{
  "framework": "nextjs",
  "installCommand": "cd ../.. && npm i -g pnpm && pnpm install",
  "buildCommand": "cd ../.. && pnpm db:generate && pnpm --filter web build",
  "rootDirectory": "apps/web"
}
